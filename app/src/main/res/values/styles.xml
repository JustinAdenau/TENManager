<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorGreen</item>
        <item name="colorPrimaryDark">@color/colorGreenDark</item>
        <item name="colorAccent">@color/colorGreyDark</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ButtonDelete" parent="Base.TextAppearance.AppCompat.Button" >
        <item name="backgroundTint">@color/colorRed</item>
    </style>

    <style name="NavigationDrawerStyle">
        <item name="android:textSize">30sp</item><!-- text size in menu-->
        <item name="android:listPreferredItemHeightSmall">70dp</item><!-- item size in menu-->
        <item name="listPreferredItemHeightSmall">70dp</item><!-- item size in menu-->
        <item name="android:textColor">@color/colorGreyDark</item>
    </style>

</resources>
